# CBHCLI v4.8.4 更新日志

## 修改内容
1. **新增** `markdown_renderer.py`（592行）— Markdown 流式渲染器 + LaTeX 公式转 Unicode
2. **修改** `ai_handler.py` — 用 `MarkdownStreamRenderer` 替换 `MarkdownTableBuffer`
3. **修改** `constants.py` — `MAX_TOOL_ROUNDS` 从 99 改为 99999
4. **版本号** 4.8.2 → 4.8.3 → 4.8.4（6 个文件 7 处）

## 版本号修改清单（v4.8.4）

| # | 文件 | 说明 |
|---|------|------|
| 1 | `cbhcli_pkg/__init__.py` | 注释 + `__version__` |
| 2 | `pyproject.toml` | `version` |
| 3 | `cbhcli_pkg/core/mcp_client.py` | MCP 握手版本 |
| 4 | `cbhcli_pkg/web/server.py` | FastAPI 版本 |
| 5 | `README.md` | 标题 + 安装命令 |

## 打包安装
```bash
cd /media/chenbh17/cbhssd/cbhcli/cbhcli-latest-Copy1
python -m build
pip install dist/cbhcli-4.8.4-py3-none-any.whl --force-reinstall --no-deps
```
