# v5.0.3 更新日志 - Web 弹窗修复 + 静态资源缓存失效 + max_tokens 可选配置

## 概述

v5.0.3 包含三项修复/新增，涉及 5 个文件。以下按文件逐一列出改动前后的代码和行号，可直接在其他机器上按此修改。

---

## 文件 1：`cbhcli_pkg/core/model.py`

### 改动 ①：`__init__` 方法新增 max_tokens 读取（第 29-30 行）

在 `self.supports_vision = model_config.get("vision", False)` 之后、`self.supports_reasoning = False` 之前插入：

```python
        # 是否支持视觉（图片输入）
        self.supports_vision = model_config.get("vision", False)
        
        # 最大输出 token 数（可选，未设置则不传给 API，使用 API 默认值）
        self.max_tokens = model_config.get("max_tokens")
        
        # 是否支持思考模式（动态检测：一旦模型返回 reasoning_content 就自动标记）
```

### 改动 ②：`chat()` 方法 payload 构造（第 126-127 行）

在 `payload = { ... }` 之后、`response = self._session.post(` 之前插入：

```python
        payload = {
            "model": self.model_name,
            "messages": self._clean_messages(messages),
            "temperature": self._get_temperature(temperature),
            **kwargs
        }
        if self.max_tokens:
            payload["max_tokens"] = self.max_tokens
        
        response = self._session.post(
```

### 改动 ③：`chat_stream()` 方法 payload 构造（第 174-175 行）

同上，在 `payload = { ... }` 之后、`response = self._session.post(` 之前插入：

```python
        payload = {
            "model": self.model_name,
            "messages": self._clean_messages(messages),
            "temperature": self._get_temperature(temperature),
            "stream": True,
            **kwargs
        }
        if self.max_tokens:
            payload["max_tokens"] = self.max_tokens
        
        response = self._session.post(
```

---

## 文件 2：`cbhcli_pkg/commands/model_cmd.py`

### 改动 ①：`_add_model()` 新增 max_tokens 输入（第 114-141 行）

在 `vision_str = ...` / `supports_vision = ...` 之后，`model_config = {` 之前插入 max_tokens 输入逻辑；在 `model_config` 字典后加条件写入；在返回信息中加 max_tokens 显示。

**插入的输入逻辑**（第 114-122 行）：

```python
    max_tokens_str = ask_text("最大输出token数 (留空用API默认值, 思考模型建议设置如 8192): ").strip()
    max_tokens = None
    if max_tokens_str:
        try:
            max_tokens = int(max_tokens_str)
            if max_tokens <= 0:
                return "❌ max_tokens 必须大于 0"
        except ValueError:
            return "❌ max_tokens 必须是数字"
```

**model_config 字典后新增**（第 134-135 行，在 `if temperature is not None:` 之后）：

```python
    if max_tokens is not None:
        model_config["max_tokens"] = max_tokens
```

**返回信息新增**（第 140-141 行，在 `vision_info = ...` 之后）：

```python
    max_tokens_info = f"   max_tokens: {max_tokens}" if max_tokens is not None else "   max_tokens: 使用API默认值"
    return f"✅ 模型 '{name}' 已添加\n   模型ID: {model_id}\n   上下文限制: {context_limit:,} tokens\n{temp_info}\n   视觉: {vision_info}\n{max_tokens_info}"
```

### 改动 ②：`_list_models()` 显示 max_tokens（第 169-171 行）

在 `lines.append(f"    视觉: {'✅ 支持' if vision else '❌ 不支持'}")` 之后、`lines.append("")` 之前插入：

```python
        vision = m.get('vision', False)
        lines.append(f"    视觉: {'✅ 支持' if vision else '❌ 不支持'}")
        max_tokens = m.get('max_tokens')
        if max_tokens is not None:
            lines.append(f"    max_tokens: {max_tokens}")
        lines.append("")
```

### 改动 ③：`_model_info()` 显示 max_tokens（第 301-305 行）

在 `lines.append(f"  视觉: {vision}")` 之后插入：

```python
    vision = "✅ 支持" if app.llm_client.supports_vision else "❌ 不支持"
    lines.append(f"  视觉: {vision}")
    max_tokens = app.llm_client.max_tokens
    if max_tokens is not None:
        lines.append(f"  max_tokens: {max_tokens}")
    else:
        lines.append(f"  max_tokens: 使用API默认值")
```

### 改动 ④：`_config_model()` 新增 max_tokens 修改项（第 399-414 行）

在 `vision_str` 修改逻辑之后、`# 保存配置` 之前插入：

```python
    # max_tokens 参数
    current_max_tokens = model_config.get('max_tokens')
    max_tokens_display = str(current_max_tokens) if current_max_tokens is not None else "使用API默认值"
    max_tokens_str = ask_text(f"最大输出token数 (当前: {max_tokens_display}, 输入 none 清除): ").strip()
    if max_tokens_str:
        if max_tokens_str.lower() == 'none' or max_tokens_str == '-':
            model_config.pop('max_tokens', None)
            print("  -> 已清除，将使用API默认值")
        else:
            try:
                new_max_tokens = int(max_tokens_str)
                if new_max_tokens <= 0:
                    return "❌ max_tokens 必须大于 0"
                model_config['max_tokens'] = new_max_tokens
            except ValueError:
                return "❌ max_tokens 必须是数字（输入 none 可清除）"
```

### 改动 ⑤：`_config_model()` 结果显示 max_tokens（第 435-439 行）

在 `lines.append(f"   视觉: {vision}")` 之后插入：

```python
    vision = "✅ 支持" if model_config.get('vision', False) else "❌ 不支持"
    lines.append(f"   视觉: {vision}")
    max_tokens = model_config.get('max_tokens')
    if max_tokens is not None:
        lines.append(f"   max_tokens: {max_tokens}")
    else:
        lines.append(f"   max_tokens: 使用API默认值")
```

---

## 文件 3：`cbhcli_pkg/web/server.py`

### 改动：`ModelConfig` 类新增字段（第 777 行）

在 `temperature: Optional[float] = None` 之后新增一行：

```python
class ModelConfig(BaseModel):
    name: str
    apiKey: str
    url: str
    model: str
    context_limit: int = 128000
    vision: bool = False
    temperature: Optional[float] = None  # 模型专属温度（None 使用全局默认）
    max_tokens: Optional[int] = None  # 最大输出token数（None 使用API默认值）
```

`to_config_dict()` 方法使用 `exclude_none=True`，None 时自动忽略，无需修改。

---

## 文件 4：`cbhcli_pkg/web/static/js/app.js`

### 改动 ①：`showSpecialModelForm()` rows 数组展开修复（第 2187 行）

```diff
- body: el("div", null, rows),
+ body: el("div", null, ...rows),
```

### 改动 ②：`showModelForm()` 新增 maxTokensInput 变量（第 2231-2234 行）

在 `visionChk.checked = !!m.vision;` 之后、`openModal({` 之前插入：

```javascript
  const visionChk = el("input", { type: "checkbox" });
  visionChk.checked = !!m.vision;
  const maxTokensInput = el("input", {
    class: "input", type: "number", min: "1",
    value: m.max_tokens ?? "", placeholder: "留空用API默认值",
  });

  openModal({
```

### 改动 ③：`showModelForm()` 表单布局新增 max_tokens 行（第 2248-2250 行）

在温度 form-hint 行 `))` 之后、视觉 checkbox 行之前插入：

```javascript
          el("div", { class: "form-hint" }, "留空用全局值 0.1；部分模型有强制要求（如 kimi-k3 必须为 1）")),
        el("div", { class: "form-row" }, el("label", null, "max_tokens"),
          maxTokensInput,
          el("div", { class: "form-hint" }, "留空用API默认值；思考模型建议设置（如 8192）")),
        el("div", { class: "form-row" }, el("label", null, " "),
```

### 改动 ④：`showModelForm()` payload 提交逻辑新增 max_tokens（第 2272-2277 行）

在 `payload.temperature = t; }` 之后、`if (!payload.name` 之前插入：

```javascript
          if (tempVal !== "") {
            const t = parseFloat(tempVal);
            if (isNaN(t) || t < 0 || t > 2) { toast("温度需在 0-2 之间", "warn"); return; }
            payload.temperature = t;
          }
          const maxTokensVal = maxTokensInput.value.trim();
          if (maxTokensVal !== "") {
            const mt = parseInt(maxTokensVal);
            if (isNaN(mt) || mt <= 0) { toast("max_tokens 需为正整数", "warn"); return; }
            payload.max_tokens = mt;
          }
          if (!payload.name || !payload.apiKey || !payload.url || !payload.model) {
```

---

## 文件 5：`cbhcli_pkg/web/static/index.html`

### 改动：3 处静态资源引用加版本号缓存失效参数

**第 8 行**：
```diff
- <link rel="stylesheet" href="/css/style.css" />
+ <link rel="stylesheet" href="/css/style.css?v=5.0.3" />
```

**第 115 行**：
```diff
- <script src="/vendor/marked.umd.js"></script>
+ <script src="/vendor/marked.umd.js?v=5.0.3"></script>
```

**第 116 行**：
```diff
- <script src="/js/app.js"></script>
+ <script src="/js/app.js?v=5.0.3"></script>
```

⚠️ 后续每次改版本号时，这 3 处 `?v=X.Y.Z` 也需同步更新。

---

## 背景：max_tokens 问题排查过程

### 现象

cbhcli 使用 glm-5.2（火山引擎 ARK API）时，复杂任务中 AI 回复中途断掉（"思考中断"）。

### 排查

**第一步：排除 API 超时**

`API_TIMEOUT = 120` 秒，流式请求中 `requests` 的 `timeout` 是每次 socket read 的超时--收到任何一块数据就重置。思考内容在持续流的话 120 秒不会触发。超时不是原因。

**第二步：API 测试确认根因**

```python
# 不设 max_tokens，发一个需要大量思考的请求
payload = {"model": "glm-5.2", "messages": [...], "stream": True}
# 统计结果：
# reasoning_content: 11143 字符
# content: 0 字符              ← 实际回复一个字都没生成
# finish_reason: length        ← 被 token 上限截断
# 最大数据间隔: 2.7s           ← 远低于 120s，与超时无关
```

**结论**：API 默认 `max_tokens`（约 4096），思考模型的 `reasoning_content` 计入这个额度。复杂任务思考内容耗尽额度后，实际回复（content）还没开始生成就被 `finish_reason: "length"` 截断。

**第三步：验证 max_tokens 参数行为**

```
max_tokens=100000  → ✅ 正常（API 自动截断到模型上限）
max_tokens=999999  → ❌ 400 报错（上限 128000）
max_tokens=8192    → ✅ 正常
```

不同 API 的上限不同，不支持该参数的 API 也可能报错。不能写死一个大值。

### 解决方案

把 `max_tokens` 做成模型配置的可选字段（与 `context_limit`、`temperature`、`vision` 同级）：
- 配了 -> payload 带上该值
- 没配 -> payload 不带，走 API 默认值
- 存量模型 -> 自动走 API 默认值，无需操作

---

## 版本号同步（6 文件 7 处 + index.html 3 处）

| # | 文件 | 内容 |
|---|------|------|
| 1 | `cbhcli_pkg/__init__.py` 第2行 | 注释 `v5.0.3` |
| 2 | `cbhcli_pkg/__init__.py` 第6行 | `__version__ = "5.0.3"` |
| 3 | `pyproject.toml` 第7行 | `version = "5.0.3"` |
| 4 | `cbhcli_pkg/core/mcp_client.py` 第202行 | MCP 握手 `"version": "5.0.3"` |
| 5 | `cbhcli_pkg/web/server.py` 第75行 | `FastAPI(..., version="5.0.3")` |
| 6 | `README.md` 第1行、安装命令行 | `# CBHCLI v5.0.3` / `pip install dist/cbhcli-5.0.3-...` |
| 7 | `cbhcli_pkg/web/static/index.html` | `?v=5.0.3`（第 8/115/116 行，共 3 处） |
